# ডেটা সিঙ্ক দ্রুত রেফারেন্স গাইড

## ডেটা ফ্লো চার্ট

```
┌─────────────────────────────────────────────────────────┐
│           ইনভেন্টরি এডিট ফর্ম                          │
│    (src/components/Inventory.tsx)                      │
└──────────────────┬──────────────────────────────────────┘
                   │ updateProduct()
                   ▼
┌─────────────────────────────────────────────────────────┐
│      প্রোডাক্ট আপডেট মিউটেশন                        │
│    (convex/products.ts - update)                       │
│  • নাম, ব্র্যান্ড, মূল্য, স্টক লেভেল                 │
│  • branchStock সব শাখায় আপডেট হয়                  │
└──────────────────┬──────────────────────────────────────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
     ┌────────┐┌────────┐┌─────────────┐
     │ মূল্য │││ স্টক ││ শাখা স্টক │
     │আপডেট││লেভেল ││ আপডেট    │
     └───┬────┘└───┬────┘└──────┬──────┘
        │         │           │
        └────┬────┴───────────┘
            ▼
    ┌───────────────────┐
    │ ড্যাশবোর্ড আপডেট│
    │  • totalInventory│
    │    Value        │
    │  • lowStockCount│
    │  • totalAbayas  │
    └───────────────────┘


┌──────────────────────────────────────────────────────────┐
│           বিক্রয় সিস্টেম (POS)                         │
│    (src/components/EnhancedPOS.tsx)                     │
└──────────────────┬──────────────────────────────────────┘
                   │ createSale()
                   ▼
┌──────────────────────────────────────────────────────────┐
│      বিক্রয় তৈরি মিউটেশন                            │
│    (convex/sales.ts - create)                          │
│  প্রতিটি প্রোডাক্টের জন্য:                           │
│  • currentStock কমায়                                   │
│  • branchStock কমায়                                    │
│  • stockMovements লগ করে                              │
│  • কাস্টমার ডেটা আপডেট করে                          │
└──────────────────┬──────────────────────────────────────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
   ┌────────┐┌────────┐┌─────────────┐
   │গ্লোবাল││শাখা   ││ স্টক মুভমেন│
   │স্টক  ││স্টক   ││ ট লগ      │
   │হ্রাস ││হ্রাস  ││            │
   └───┬────┘└───┬────┘└──────┬──────┘
      │         │           │
      └────┬────┴───────────┘
          ▼
   ┌───────────────────┐
   │ ড্যাশবোর্ড আপডেট│
   │  • totalRevenue   │
   │  • orders count   │
   │  • stock         │
   └───────────────────┘
```

---

## প্রতিটি অপারেশনে আপডেট হওয়া টেবিল

### 1️⃣ প্রোডাক্ট এডিট

**যখন**: ইনভেন্টরিতে প্রোডাক্ট এডিট করা হয়

**কোন টেবিল আপডেট হয়**:
- ✅ `products` - মূল রেকর্ড
  - `name`, `brand`, `model`
  - `fabric`, `color`, `style`, `occasion`
  - `costPrice`, `sellingPrice`
  - `minStockLevel`, `maxStockLevel`
  - `sizes`, `embellishments`, `description`
  - `barcode`, `productCode`, `madeBy`
  - `branchStock` (সব শাখার লেভেল আপডেট)
  - `isActive`

**রিয়েল-টাইম প্রভাব**:
- ⚡ ড্যাশবোর্ড: `totalInventoryValue` পুনরায় গণনা
- ⚡ স্টক ম্যানেজমেন্ট: স্টক লেভেল আপডেট দেখায়
- ⚡ রিপোর্ট: মূল্য এবং লাভ মার্জিন আপডেট

---

### 2️⃣ বিক্রয় তৈরি

**যখন**: POS থেকে বিক্রয় সম্পন্ন করা হয়

**কোন টেবিল আপডেট হয়**:
- ✅ `sales` - নতুন বিক্রয় রেকর্ড
- ✅ `products` - প্রতিটি প্রোডাক্টের জন্য:
  - `currentStock` কমে যায়
  - `branchStock[branchId].currentStock` কমে যায়
- ✅ `stockMovements` - প্রতিটি প্রোডাক্টের জন্য লেনদেন লগ
- ✅ `customers` (যদি কাস্টমার নির্বাচিত হয়):
  - `totalPurchases` বৃদ্ধি পায়
  - `lastPurchaseDate` আপডেট হয়

**রিয়েল-টাইম প্রভাব**:
- ⚡ ইনভেন্টরি: স্টক তাৎক্ষণিকভাবে হ্রাস পায়
- ⚡ স্টক ম্যানেজমেন্ট: শাখার স্টক আপডেট হয়
- ⚡ ড্যাশবোর্ড: `totalRevenue`, `todaysSales` আপডেট হয়
- ⚡ রিপোর্ট: বিক্রয় রিপোর্ট তাজা করা হয়

---

### 3️⃣ স্টক সামঞ্জস্য

**যখন**: স্টক ম্যানেজমেন্টে স্টক ম্যানুয়ালি সামঞ্জস্য করা হয়

**কোন টেবিল আপডেট হয়**:
- ✅ `products`:
  - `currentStock` নতুন মানে সেট হয়
  - `branchStock[defaultBranchId].currentStock` আপডেট হয়
- ✅ `stockMovements` - লেনদেন লগ করা হয়

**রিয়েল-টাইম প্রভাব**:
- ⚡ ইনভেন্টরি: স্টক আপডেট দেখায়
- ⚡ স্টক ম্যানেজমেন্ট: নতুন স্টক দেখা যায়
- ⚡ ড্যাশবোর্ড: `totalAbayas`, `totalInventoryValue` আপডেট হয়

---

### 4️⃣ স্টক ট্রান্সফার (শিপ করা)

**যখন**: স্টক ট্রান্সফার অনুমোদিত এবং শিপ করা হয়

**কোন টেবিল আপডেট হয়**:
- ✅ `stockTransfer` - ট্রান্সফার স্ট্যাটাস = "completed"
- ✅ প্রতিটি প্রোডাক্টের জন্য:
  - **উৎস শাখা**: `branchStock[sourceId].currentStock` কমে যায়
  - **গন্তব্য শাখা**: `branchStock[destId].currentStock` বৃদ্ধি পায়
  - **গ্লোবাল**: `currentStock` অপরিবর্তিত থাকে
- ✅ `inventoryTransactions` - লেনদেন লগ

**রিয়েল-টাইম প্রভাব**:
- ⚡ স্টক ম্যানেজমেন্ট: উভয় শাখার স্টক আপডেট হয়
- ⚡ রিপোর্ট: শাখা-অনুযায়ী স্টক সঠিক থাকে

---

### 5️⃣ হোয়াটসঅ্যাপ অর্ডার ডেলিভারি

**যখন**: WhatsApp অর্ডার চিহ্নিত করা হয় "delivered"

**কোন টেবিল আপডেট হয়**:
- ✅ `whatsappOrders` - স্ট্যাটাস = "delivered"
- ✅ `sales` - নতুন বিক্রয় রেকর্ড তৈরি
- ✅ প্রতিটি প্রোডাক্টের জন্য:
  - `currentStock` কমে যায়
  - `branchStock` কমে যায়
- ✅ `stockMovements` - লেনদেন লগ

---

## সঠিকতা চেকলিস্ট

### প্রোডাক্ট লেভেল ডেটা
- [ ] `products.costPrice` সঠিক
- [ ] `products.sellingPrice` সঠিক
- [ ] `products.currentStock` = `branchStock[*].currentStock` এর যোগফল
- [ ] `products.minStockLevel` = সব `branchStock[*].minStockLevel`
- [ ] `products.maxStockLevel` = সব `branchStock[*].maxStockLevel`

### শাখা লেভেল ডেটা
- [ ] প্রতিটি শাখার `branchStock.currentStock` >= 0
- [ ] `branchStock.minStockLevel` গ্লোবাল মানের সাথে মেলে
- [ ] `branchStock.maxStockLevel` গ্লোবাল মানের সাথে মেলে

### ড্যাশবোর্ড গণনা
- [ ] `totalInventoryValue` = Σ(costPrice × currentStock)
- [ ] `lowStockCount` = গণনা যেখানে currentStock ≤ minStockLevel
- [ ] `totalAbayas` = Σ(currentStock) সক্রিয় প্রোডাক্ট
- [ ] `totalRevenue` = Σ(sales.total)

---

## সাধারণ সমস্যা এবং সমাধান

### সমস্যা: ড্যাশবোর্ডে totalInventoryValue আপডেট না হওয়া
**কারণ**: মূল্য পরিবর্তন সম্পূর্ণ হয়নি
**সমাধান**: পেজ রিফ্রেশ করুন (ড্যাশবোর্ড লাইভ কোয়েরি)

### সমস্যা: স্টক ম্যানেজমেন্টে শাখার স্টক সঠিক না হওয়া
**কারণ**: `branchStock` অ্যারে সিঙ্ক না হওয়া
**সমাধান**: প্রোডাক্ট এডিট করে স্টক লেভেল পুনরায় সংরক্ষণ করুন

### সমস্যা: লো স্টক ওয়ার্নিং কাজ না করা
**কারণ**: `minStockLevel` সব শাখায় সিঙ্ক না হওয়া
**সমাধান**: উপরের ফিক্স প্রয়োগ করুন

---

## কোড রেফারেন্স

### প্রোডাক্ট আপডেট
📁 `convex/products.ts` - `update` মিউটেশন (লাইন ২৪১)

### বিক্রয় তৈরি
📁 `convex/sales.ts` - `create` মিউটেশন (লাইন ৩৯)

### স্টক সামঞ্জস্য
📁 `convex/products.ts` - `adjustStock` মিউটেশন (লাইন ৩৮৬)

### ড্যাশবোর্ড কোয়েরি
📁 `convex/dashboard.ts` - `getStats` কোয়েরি (লাইন ৪)

### ইনভেন্টরি এডিট ফর্ম
📁 `src/components/Inventory.tsx` - `handleUpdateProduct` ফাংশন (লাইন ৩৩০)

---

**আপডেট: ২০২৬-০১-৩০** ✅
