# 📷 ক্যামেরা ক্যাপচার সিস্টেম - সম্পূর্ণ গাইড

## বৈশিষ্ট্য সারসংক্ষেপ

### ✅ কী যোগ করা হয়েছে:

1. **🎥 প্রোডাক্ট ইমেজ রিকগনিশন এ ক্যামেরা সাপোর্ট**
   - লাইভ ভিডিও স্ট্রীম
   - স্ন্যাপশট ক্যাপচার
   - ক্যামেরা পরিবর্তন (সামনে/পিছনে)
   - তাৎক্ষণিক ছবি বিশ্লেষণ

2. **🪝 useCamera Hook**
   - পুনঃব্যবহারযোগ্য ক্যামেরা লজিক
   - সব ত্রুটি হ্যান্ডলিং
   - ক্যামেরা পারমিশন ম্যানেজমেন্ট

3. **🔧 CameraService**
   - স্ট্রিম ম্যানেজমেন্ট
   - ফ্রেম ক্যাপচার
   - মেটাডেটা এক্সট্রাক্ট
   - ব্যবহারকারী-বান্ধব ত্রুটি বার্তা

4. **🎨 CameraCapture কম্পোনেন্ট**
   - স্ট্যান্ডালোন ক্যামেরা UI
   - প্রিভিউ এবং গ্রহণ লজিক
   - মাল্টি-ক্যামেরা সাপোর্ট

---

## ফাইল সংগঠন

```
src/
├── components/
│   ├── StaffPortal/
│   │   └── ProductImageRecognition.tsx  ✨ ক্যামেরা যোগ করা হয়েছে
│   └── CameraCapture.tsx                🆕 নতুন ক্যামেরা কম্পোনেন্ট
├── hooks/
│   └── useCamera.ts                     🆕 নতুন ক্যামেরা হুক
└── services/
    └── CameraService.ts                 🆕 নতুন ক্যামেরা সার্ভিস
```

---

## ProductImageRecognition এ ক্যামেরা ইন্টিগ্রেশন

### নতুন ফিচার:

```tsx
// ক্যামেরা বাটন
<button onClick={startCamera}>
  <Camera className="w-5 h-5" />
  ক্যামেরা দিয়ে ছবি তুলুন
</button>

// ক্যামেরা মোডাল
{showCamera && (
  <div className="fixed inset-0 bg-black bg-opacity-75 ...">
    {/* লাইভ ভিডিও */}
    <video ref={videoRef} autoPlay playsInline />
    
    {/* ক্যাপচার বাটন */}
    <button onClick={captureSnapshot}>
      <Camera className="w-5 h-5" />
      ছবি তুলুন
    </button>
  </div>
)}
```

### স্টেট ম্যানেজমেন্ট:

```typescript
const [showCamera, setShowCamera] = useState(false);      // মোডাল দৃশ্যমানতা
const [cameraActive, setCameraActive] = useState(false);  // স্ট্রীম সক্রিয়
const [uploadedImage, setUploadedImage] = useState(null); // ক্যাপচার করা ছবি

const videoRef = useRef<HTMLVideoElement>(null);        // ভিডিও উপাদান
const canvasRef = useRef<HTMLCanvasElement>(null);      // ক্যানভাস উপাদান
const streamRef = useRef<MediaStream | null>(null);     // মিডিয়া স্ট্রীম
```

### মূল ফাংশন:

```typescript
// ক্যামেরা শুরু করুন
const startCamera = async () => {
  const stream = await navigator.mediaDevices.getUserMedia({
    video: { 
      facingMode: 'environment',  // পিছনের ক্যামেরা
      width: { ideal: 1280 },
      height: { ideal: 720 }
    }
  });
  videoRef.current.srcObject = stream;
  streamRef.current = stream;
};

// স্ন্যাপশট ক্যাপচার করুন
const captureSnapshot = () => {
  const context = canvasRef.current.getContext('2d');
  canvasRef.current.width = videoRef.current.videoWidth;
  canvasRef.current.height = videoRef.current.videoHeight;
  context.drawImage(videoRef.current, 0, 0);
  
  const imageUrl = canvasRef.current.toDataURL('image/jpeg', 0.9);
  setUploadedImage(imageUrl);
  recognizeProduct(imageUrl);  // স্বয়ংক্রিয় স্বীকৃতি
};

// ক্যামেরা বন্ধ করুন
const stopCamera = () => {
  streamRef.current?.getTracks().forEach(track => track.stop());
};
```

---

## useCamera Hook ব্যবহার

### ব্যবহার উদাহরণ:

```tsx
import { useCamera } from '@/hooks/useCamera';

export const MyComponent = () => {
  const { 
    videoRef, 
    canvasRef, 
    isActive, 
    error,
    startCamera, 
    stopCamera, 
    capturePhoto,
    switchCamera,
    getAvailableCameras
  } = useCamera();

  return (
    <>
      <button onClick={() => startCamera()}>
        ক্যামেরা শুরু করুন
      </button>
      
      <video ref={videoRef} autoPlay playsInline />
      <canvas ref={canvasRef} className="hidden" />
      
      <button onClick={() => {
        const photo = capturePhoto();
        stopCamera();
      }}>
        ছবি তুলুন
      </button>
    </>
  );
};
```

### Hook API:

```typescript
interface UseCameraReturn {
  videoRef: React.RefObject<HTMLVideoElement>;
  canvasRef: React.RefObject<HTMLCanvasElement>;
  isActive: boolean;                  // ক্যামেরা চলছে?
  error: string | null;               // ত্রুটি বার্তা
  startCamera: (options?) => Promise<void>;
  stopCamera: () => void;
  capturePhoto: () => string | null;  // Base64 URL
  switchCamera: () => Promise<void>;  // সামনে/পিছনে
  getAvailableCameras: () => Promise<MediaDeviceInfo[]>;
}
```

---

## CameraService ব্যবহার

### স্ট্যাটিক মেথড:

```typescript
import { CameraService, CapturedPhoto } from '@/services/CameraService';

// ক্যামেরা উপলব্ধতা চেক করুন
if (CameraService.isSupported()) {
  console.log('ক্যামেরা সাপোর্ট করা হয়');
}

// স্ট্রীম শুরু করুন
const stream = await CameraService.startStream(videoElement, 'environment');

// ফটো ক্যাপচার করুন
const photo = CameraService.captureFrame(videoElement, canvasElement, 0.9);

// মেটাডেটা পান
const meta = CameraService.getPhotoMetadata(photo);
console.log(meta.dimensions);   // "1280x720"
console.log(meta.fileSize);     // "250 KB"
console.log(meta.timestamp);    // "২/৩/২০২৬ ১০:৩০:৪৫"

// ডাউনলোড করুন
CameraService.downloadPhoto(photo, 'product-photo.jpg');

// Blob এ রূপান্তর করুন
const blob = await CameraService.photoToBlob(photo);

// স্ট্রীম বন্ধ করুন
CameraService.stopStream(stream);

// ত্রুটি হ্যান্ডলিং
try {
  const stream = await CameraService.startStream(videoEl, 'environment');
} catch (error) {
  const message = CameraService.getErrorMessage(error);
  console.error(message);  // "ক্যামেরা অ্যাক্সেস অনুমোদিত হয়নি"
}
```

### CapturedPhoto ইন্টারফেস:

```typescript
interface CapturedPhoto {
  dataUrl: string;        // Base64 ডেটা URL
  timestamp: Date;        // ক্যাপচারের সময়
  width: number;         // প্রস্থ পিক্সেলে
  height: number;        // উচ্চতা পিক্সেলে
  size: number;          // ফাইল সাইজ বাইটে
  mimeType: string;      // "image/jpeg"
}
```

---

## CameraCapture কম্পোনেন্ট

### স্ট্যান্ডালোন ব্যবহার:

```tsx
import { CameraCapture } from '@/components/CameraCapture';
import { CapturedPhoto } from '@/services/CameraService';

export const MyComponent = () => {
  const handleCapture = (photo: CapturedPhoto) => {
    console.log('ছবি ক্যাপচার হয়েছে:', photo.dataUrl);
    // পণ্য স্বীকৃতিতে পাঠান
  };

  return (
    <CameraCapture 
      onCapture={handleCapture}
      onClose={() => console.log('বন্ধ করুন')}
      facingMode="environment"
      autoStart={true}
    />
  );
};
```

### Props:

```typescript
interface CameraCaptureProps {
  onCapture: (photo: CapturedPhoto) => void;  // বাধ্যতামূলক
  onClose?: () => void;                        // ঐচ্ছিক
  facingMode?: 'user' | 'environment';        // ডিফল্ট: 'environment'
  autoStart?: boolean;                        // ডিফল্ট: true
}
```

---

## ত্রুটি হ্যান্ডলিং

### সাধারণ ত্রুটি:

```typescript
// NotAllowedError
// → "ক্যামেরা অ্যাক্সেস অনুমোদিত হয়নি"

// NotFoundError
// → "এই ডিভাইসে কোনো ক্যামেরা পাওয়া যায়নি"

// NotReadableError
// → "ক্যামেরা অন্য অ্যাপ্লিকেশনে ব্যবহৃত হচ্ছে"

// SecurityError
// → "সিকিউরিটি কারণে ক্যামেরা অ্যাক্সেস অনুমোদিত নয়"

// TypeError
// → "এই ব্রাউজারে ক্যামেরা সাপোর্ট করা হয় না"
```

### এরর ম্যানেজমেন্ট:

```tsx
try {
  const stream = await CameraService.startStream(videoEl, 'environment');
} catch (err) {
  const errorMsg = CameraService.getErrorMessage(err);
  toast.error(errorMsg);
  setError(errorMsg);
}
```

---

## ব্রাউজার সামঞ্জস্য

| ব্রাউজার | সাপোর্ট | নোট |
|----------|---------|------|
| Chrome | ✅ সম্পূর্ণ | সব ফিচার সমর্থিত |
| Firefox | ✅ সম্পূর্ণ | সব ফিচার সমর্থিত |
| Safari | ✅ সম্পূর্ণ | iOS 14.5+ প্রয়োজন |
| Edge | ✅ সম্পূর্ণ | Chromium-ভিত্তিক |
| IE 11 | ❌ অসমর্থিত | Web API সাপোর্ট নেই |

### HTTPS প্রয়োজনীয়তা:

```
⚠️ ক্যামেরা API শুধুমাত্র HTTPS এ কাজ করে
   (localhost এ HTTP অনুমোদিত)
```

---

## পারফরম্যান্স অপটিমাইজেশন

### ছবি গুণমান:

```typescript
// উচ্চ মান (ছোট ফাইল)
canvasElement.toDataURL('image/jpeg', 0.9);

// মধ্যম মান
canvasElement.toDataURL('image/jpeg', 0.7);

// কম মান (সবচেয়ে ছোট)
canvasElement.toDataURL('image/jpeg', 0.5);
```

### ক্যামেরা রেজোলিউশন:

```typescript
// উচ্চ রেজোলিউশন
{ video: { width: { ideal: 1920 }, height: { ideal: 1080 } } }

// মধ্যম রেজোলিউশন (ডিফল্ট)
{ video: { width: { ideal: 1280 }, height: { ideal: 720 } } }

// কম রেজোলিউশন
{ video: { width: { ideal: 640 }, height: { ideal: 480 } } }
```

---

## ডেটা ফ্লো

### প্রোডাক্ট ইমেজ রিকগনিশন ফ্লো:

```
ব্যবহারকারী ক্যামেরা বাটন ক্লিক করেন
    ↓
startCamera() সক্রিয় হয়
    ↓
ভিডিও স্ট্রীম প্রদর্শিত হয় মোডালে
    ↓
ব্যবহারকারী ছবি তুলেন
    ↓
captureSnapshot() ক্যানভাসে ফ্রেম আঁকে
    ↓
toDataURL() ছবিকে Base64 এ রূপান্তরিত করে
    ↓
setUploadedImage() অবস্থা আপডেট করে
    ↓
recognizeProduct() স্বয়ংক্রিয়ভাবে বিশ্লেষণ করে
    ↓
ফলাফল প্রদর্শিত হয়
```

---

## নিরাপত্তা বিবেচনা

### ব্যবহারকারীর গোপনীয়তা:

```
✅ ক্যামেরা শুধুমাত্র সুস্পষ্ট ব্যবহারকারী অনুমোদনে কাজ করে
✅ কোনো ছবি সার্ভারে সংরক্ষিত হয় না (শুধু বিশ্লেষণ)
✅ ক্লায়েন্ট-সাইড প্রসেসিং
✅ HTTPS এনক্রিপশন প্রয়োজনীয়
```

### সেরা অনুশীলন:

```typescript
// সর্বদা ক্লিনআপ করুন
useEffect(() => {
  return () => {
    if (streamRef.current) {
      streamRef.current.getTracks().forEach(track => track.stop());
    }
  };
}, []);

// ত্রুটি হ্যান্ডলিং
try {
  const stream = await CameraService.startStream(videoEl);
} catch (error) {
  const msg = CameraService.getErrorMessage(error);
  console.error(msg);
}

// পারমিশন চেক করুন
if (CameraService.isSupported()) {
  // ক্যামেরা ফিচার সক্রিয় করুন
}
```

---

## সাধারণ প্রশ্ন

### Q: ক্যামেরা অ্যাক্সেস অনুমোদিত হয়নি?
**A:** ব্রাউজারের সেটিংসে ক্যামেরা অনুমতি সক্রিয় করুন।

### Q: কোন ক্যামেরা তালিকা পাওয়া যাচ্ছে না?
**A:** `getAvailableCameras()` ব্যবহার করুন মাল্টি-ক্যামেরা ডিভাইসের জন্য।

### Q: ছবির গুণমান খারাপ?
**A:** জুম বাড়ান এবং ভালো আলোতে রাখুন। `toDataURL()` এ quality বৃদ্ধি করুন।

### Q: HTTPS ছাড়াই কাজ করছে না?
**A:** ক্যামেরা API শুধুমাত্র HTTPS এ কাজ করে (localhost ব্যতিক্রম)।

---

## ভবিষ্যত উন্নতি

- [ ] QR কোড স্ক্যানিং
- [ ] বারকোড স্বীকৃতি
- [ ] রিয়েল-টাইম ফিল্টার
- [ ] মাল্টিপল ক্যামেরা নির্বাচন
- [ ] ভিডিও রেকর্ডিং

---

**সংস্করণ**: 1.0  
**স্থিতি**: ✅ উৎপাদনের জন্য প্রস্তুত  
**শেষ আপডেট**: ফেব্রুয়ারি ২০২৬
